const header = document.getElementById("main-header");
console.log(header);

const firstParagraph = document.querySelector(selectors 'p');

const allButtons= document.querySelectorAll(selectors 'button');

console.log(allButtons);

const item: = document.getElementsByClassName(classNames: 'list-item');

console.log(items);

const images = document.getElementByTagName(qualifeiedName: 'img');

const newDiv: = cocument.createElement(tagName:'div');
newDiv.textContent = 'Hello, World!';
document.body.appendChild(newDiv);

const heading: = document.querySelector(selectors 'h1');
heading.textContent = 'Updated title';

const list:NodeListOf<HTMLUListElement> = docment.querySelector(selectors:'ul');
const firstItem = list.firstElementChild;
const firstItem = list[0].lastElementChild;
list[0].removeChild(firstItem);

const button = docment.querySelector(selectors:'button');



const sayHello = ():void => alert('Hello');
button.addEventListener(type:)
button.removeEventListener()
